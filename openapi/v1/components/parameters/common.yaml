TenantId:
  name: tenantId
  in: path
  required: true
  description: Tenant UUID
  schema:
    type: string
    format: uuid

Tenant:
  name: tenant
  in: path
  required: true
  description: Tenant UUID
  schema:
    type: string
    format: uuid

Page:
  name: page
  in: query
  schema:
    type: integer
    minimum: 1
    default: 1

PerPage:
  name: per_page
  in: query
  schema:
    type: integer
    minimum: 1
    maximum: 100
    default: 15

Search:
  name: search
  in: query
  schema: { type: string }

SortBy:
  name: sort_by
  in: query
  schema: { type: string }

SortOrder:
  name: sort_order
  in: query
  schema:
    type: string
    enum: [asc, desc]
    default: desc

MaterialId:
  name: materialId
  in: path
  required: true
  description: Material UUID
  schema:
    type: string
    format: uuid

Material:
  name: material
  in: path
  required: true
  description: Material UUID
  schema:
    type: string
    format: uuid

RecipeId:
  name: recipeId
  in: path
  required: true
  description: Recipe UUID
  schema:
    type: string
    format: uuid

Recipe:
  name: recipe
  in: path
  required: true
  description: Recipe UUID
  schema:
    type: string
    format: uuid

InventoryTransactionId:
  name: transactionId
  in: path
  required: true
  description: Inventory Transaction UUID
  schema:
    type: string
    format: uuid

ProductId:
  name: productId
  in: path
  required: true
  description: Product UUID
  schema:
    type: string
    format: uuid

Product:
  name: product
  in: path
  required: true
  description: Product UUID
  schema:
    type: string
    format: uuid

VariantId:
  name: variantId
  in: path
  required: true
  description: Variant UUID
  schema:
    type: string
    format: uuid

ImageId:
  name: imageId
  in: path
  required: true
  description: Image ID
  schema:
    type: string

SupplierId:
  name: supplierId
  in: path
  required: true
  description: Supplier UUID
  schema:
    type: string
    format: uuid

TagId:
  name: tagId
  in: path
  required: true
  description: Tag UUID
  schema:
    type: string
    format: uuid

UserId:
  name: userId
  in: path
  required: true
  description: User UUID
  schema:
    type: string
    format: uuid

CustomerId:
  name: customerId
  in: path
  required: true
  description: Customer UUID
  schema:
    type: string
    format: uuid

OrderId:
  name: orderId
  in: path
  required: true
  description: Order UUID
  schema:
    type: string
    format: uuid

AlertId:
  name: alertId
  in: path
  required: true
  description: Alert UUID
  schema:
    type: string
    format: uuid

ComponentId:
  name: component
  in: path
  required: true
  description: Recipe Component ID
  schema:
    type: string

Id:
  name: id
  in: path
  required: true
  description: Resource ID
  schema:
    type: string

Slug:
  name: slug
  in: path
  required: true
  description: Page slug
  schema:
    type: string